const UnavailabilityAdd = ({ onHandleClick }) => {
    return (
        <>
            <button type="button" onClick={onHandleClick}>Add Dates+</button>
        </>
    )
}

export default UnavailabilityAdd
